'use strict';
var assert = require('assert'),
    yaol = require('..');

describe('yaol', function() {
  
  it('must exist', function(done) {
    
    assert(typeof yaol, 'object');
        
    done();
    
  });
});
